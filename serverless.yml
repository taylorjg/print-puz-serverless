frameworkVersion: '3'
org: taylorjg
app: print-puz
service: print-puz

provider:
  name: aws
  runtime: nodejs18.x

plugins:
  - serverless-webpack

functions:
  parse-puzzle:
    handler: api/parse-puzzle.handler
    events:
      - httpApi:
          method: get
          path: /parse-puzzle
  scrape-puzzle-url:
    handler: api/scrape-puzzle-url.handler
    events:
      - httpApi:
          method: get
          path: /scrape-puzzle-url
